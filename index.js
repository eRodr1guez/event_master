import {AppRegistry} from 'react-native';
import Initializing from './components/Initializing';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Initializing);
